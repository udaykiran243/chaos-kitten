# Gitleaks configuration for chaos-kitten
# https://github.com/gitleaks/gitleaks

title = "chaos-kitten gitleaks config"

# Allowlist for JWT attack payloads in toys/
# These are intentional test payloads, not real secrets
[allowlist]
description = "JWT attack profile test payloads"
paths = [
  '''toys/jwt_attacks\.yaml'''
]
