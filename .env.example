# Copy this file to `.env` and fill in the values:
#
#   cp .env.example .env
#
# Notes:
# - Never commit your real `.env` file.
# - Chaos Kitten also expands `${VAR}` placeholders inside `chaos-kitten.yaml`.

# Anthropic Claude API key (primary LLM provider).
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API key (alternative LLM provider).
OPENAI_API_KEY=your_openai_api_key_here

# Ollama server base URL for running with a local LLM.
# Default: http://localhost:11434
OLLAMA_BASE_URL=http://localhost:11434

# Logging level (DEBUG, INFO, WARNING, ERROR).
CHAOS_KITTEN_LOG_LEVEL=INFO

# Custom config file path.
# Default: chaos-kitten.yaml
CHAOS_KITTEN_CONFIG=chaos-kitten.yaml

# Target API auth token (referenced by `chaos-kitten.yaml` as `${API_TOKEN}`).
API_TOKEN=your_target_api_token_here
